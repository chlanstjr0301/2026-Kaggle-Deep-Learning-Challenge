# 재현 환경 및 재현성 명세

**근거 규정**: 8.2 (수상 후보자는 학습/추론 코드, 학습된 모델 가중치, 데이터셋 목록 및
접근 방법, 재현 환경 설명(하드웨어·라이브러리 버전), 방법론 설명 문서 제출),
8.2b (재현이 불가능한 경우 수상 취소 가능)

---

## 1. 하드웨어

```
GPU     NVIDIA A100 SXM4 40GB  ×1   (vast.ai 대여)
드라이버 CUDA 12.x
디스크   약 100GB (모델 가중치 + 생성물)
```

최종 추론(약 2,000문항 × k=64)은 A100 40GB 1장 기준 **약 2.5시간**이 소요된다.
생성 처리량은 약 10,000 tok/s 로 관측됐다.

## 2. 소프트웨어

두 개의 파이썬 환경을 분리해 사용한다. 추론과 학습의 `transformers` 요구 버전이
충돌하기 때문이다.

```
/venv/main   (추론)
  python 3.12
  vllm 0.27.1
  transformers 5.15.1
  pandas, numpy

/venv/train  (학습)
  python 3.12  (--system-site-packages)
  transformers 4.57.6
  peft 0.20.0
  trl 1.10.0
  accelerate 1.14.0
  datasets 5.0.1
```

**이 분리는 필수다.** 학습 환경을 main 에 통합하면 vLLM 0.27.1 이 동작하지 않는다.
`ensure_train_venv.sh` 가 학습 환경을 재생성한다.

## 3. 모델 가중치

```
베이스        Qwen2.5-3B-Instruct  (공개 체크포인트, 무수정)
ORM r1        LoRA 어댑터 (r=16, alpha=32, dropout=0.05,
                            target: q,k,v,o,gate,up,down_proj)
                            학습 stance = harsh · 학습 행 26,023 · 미채택
ORM r2        동일 설정, 학습 stance = plain · 학습 행 26,034 · **최종 채택**
```

두 어댑터는 같은 학습 데이터 파일(`out/orm_r1.jsonl`)을 읽었고 학습 시 판정
stance 만 다르다. 행 수 11건 차이의 원인은 규명하지 못했다(전체의 0.04%).

병합 모델(`ckpt/orm_r2-merged`)은 **어댑터 + 베이스에서 결정적으로 재생성된다.**
`merge_orm.sh` 로 재현 검증을 완료했다. 따라서 제출물에는 병합본(약 6GB) 대신
**어댑터(수십 MB)만 포함해도 비트 단위로 동일한 모델을 복원할 수 있다.**

## 4. 실행 절차

```
bash run_final.sh <입력CSV>
```

6단계로 구성되며 각 단계에 사전점검이 있다.

```
[0] 사전점검   입력·모델·ORM config·스크립트·디스크(≥20G)·GPU 여유(≥30000MiB)
               ·잔존 VLLM/EngineCore 프로세스 확인
[1] 입력 정규화 정답열이 없는 평가셋에 자리표시자를 채우고 행 수를 별도 기록
[2] 생성       프롬프트 C · chat · k=64 · T=0.8 · max_tokens 3072
[3] ORM 채점   ckpt/orm_r2-merged
[4] 제출 생성  score(a) = log(n_a) + 0.5·z(ORM), 동률은 답 오름차순으로 결정적 처리
[5] 자체 검증  다수결 대비 뒤집힘 수가 0 이거나 25% 초과면 거부
[6] 요약
```

중단 시 **체크포인트 재개**가 가능하며, 100→150문항 재개 시 불완전 레코드 0 을 확인했다.

## 5. 재현성의 한계 — 반드시 읽을 것

**동일 입력·동일 설정·동일 시드(seed=42)로 재실행해도 출력은 비트 단위로 재현되지 않는다.**

원인은 vLLM 의 배치 스케줄링 비결정성이다. `seed` 는 샘플링만 고정하며,
배치 구성에 따른 부동소수점 누적 순서까지는 고정하지 못한다.

### 실측 —— 세 번의 독립 반복

```
동일 머신·동일 시드 재실행
  리더보드 831문항    답이 다른 문항 14 (1.7%)   점수 0.79783 → 0.79663   차 0.0012
  검증셋  1,000문항    답이 다른 문항 42 (4.2%)   maj  0.8250 → 0.8230     차 0.0020
  생성 텍스트가 동일한 문항   0 / 831

머신 재구축 후 재실행 (2026-08-29)
  리더보드 831문항    답이 다른 문항 18 (2.2%)   점수 0.79663 → 0.79783   차 0.0012
```

**세 번째가 재현 검증의 직접 근거다.** 대여 인스턴스를 파괴하고 새로 대여해
본 저장소 내용만으로 복원한 뒤 리더보드 구성을 재실행했다. 라이브러리는 전부
고정 버전으로 일치했고 PyTorch 의 CUDA 빌드만 12.8 에서 13.0 으로 바뀌었다.

바뀐 18문항의 1–2위 득표 차이는 중앙값 2, 최대 16이다. 전체 831문항의 중앙값은
59다. **불안정성은 사실상 동점인 문항에만 존재하며, 득표 차이가 16을 넘는 문항은
하나도 바뀌지 않았다.**

다수결 전용 제출본끼리 대조하면 출처가 갈린다. 18건 중 13건은 다수결 답이 먼저
바뀌어 융합이 따라간 것이고, 5건은 다수결이 유지된 상태에서 검증기가 결과를
움직인 것이다. 반대로 다수결 답이 바뀐 21문항 중 8문항에서는 융합이 답을 유지했다.
흡수 8 대 도입 5 이나 불일치쌍이 13건뿐이라 유의하지 않다(McNemar 정확검정
양측 p = 0.581).

**개별 문항의 답은 실행마다 2~4% 바뀐다. 그러나 집계 정확도는 거의 움직이지 않는다.**
뒤집힘이 정답↔오답 양방향으로 고르게 일어나 상쇄되기 때문이다.

> **정확도는 ±0.004 이내에서 재현된다고 본다.**

이 값의 한계를 명시한다. 산포를 **세 번의 측정**에서 추정했으므로 그 자체의 불확실성이 크다.
초기에는 뒤집힘 수에서 √n 으로 역산해 ±0.016 을 제시했으나, 그 방식은 개별 뒤집힘이
독립이라고 가정하여 **과대추정**이었다. 직접 측정값을 채택한다.

재현 검증 시 비트 단위 일치를 기대해서는 안 되며, 위 구간 안에 들어오면 재현된 것으로 본다.

### 이 한계가 측정에 미친 영향

- **동일 생성물 위의 짝비교는 영향받지 않는다.** 융합 이득, k 판정, γ 스윕,
  레짐 분석은 모두 단일 생성 파일의 부분표본 비교이므로 유효하다.
- **서로 다른 실행 간 비교는 무효에 가깝다.** 실행 간 잡음이 비교하려는 효과보다 크다.
  본 연구는 이를 발견한 뒤 해당 결론 하나를 "측정 불가"로 정정했다.

## 6. 결정성이 보장되는 부분

```
LoRA 병합            결정적 (merge_orm.sh 로 재현 확인)
답 추출 파서          결정적 (src/extract.py)
다수결 동률 처리      결정적 (답 오름차순)
융합 점수 계산        결정적 (주어진 생성물에 대해)
```

즉 **비결정성은 생성 단계에만 존재한다.** 동일한 생성 파일이 주어지면
채점·융합·제출 파일 생성은 완전히 재현된다.

## 7. 제출물 구성

```
code/          src/ (21개 모듈) · run_final.sh · merge_orm.sh · ensure_train_venv.sh
weights/       ckpt/orm_r1/  (LoRA 어댑터 + tokenizer)
docs/          COMPLIANCE.md · REPRODUCIBILITY.md · 방법론 문서 · DECISIONS.md
data/          외부 데이터셋 목록 및 접근 방법 (COMPLIANCE.md §1)
```

`DECISIONS.md` 는 D-9 부터 최종일까지의 의사결정 전 과정을 시간순으로 기록한 문서이며,
모든 사전등록·게이트·판정·정정이 관측 시점과 함께 남아 있다.
