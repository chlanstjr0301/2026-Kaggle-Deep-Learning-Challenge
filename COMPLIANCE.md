# 외부 데이터 및 API 사용 명세

**대회**: 제5회 대학 연합 아주 소중한 딥러닝 챌린지 2026
**팀**: chlanstjr (개인 참가)
**작성일**: 2026-08-26 (최종 갱신 2026-08-29)
**근거 규정**: 5.2c(외부 데이터셋 목록 명시), 5.3a(학습 데이터 구축용 상용 API 허용)

---

## 0. 요약 — 최종 제출 구성에 외부 데이터는 포함되지 않는다

최종 채택 구성은 다음과 같다.

```
베이스   Qwen2.5-3B-Instruct  (추가 학습 없음)
생성     프롬프트 C · chat template · k=64 · T=0.8 · max_tokens 3072
선별     ORM r2 (LoRA 검증기, 학습 stance=plain) 융합
         score(a) = log(n_a) + 0.5·z(ORM),  동률은 답 오름차순
```

- **생성 모델은 베이스 그대로다.** 어떤 학습 가중치도 병합되지 않았다.
- **ORM r2** 는 유일하게 학습된 구성요소이며, 학습 데이터는 **주최 측 제공 train 데이터로
  우리 베이스 모델이 직접 생성한 풀이**다. 외부 코퍼스나 외부 모델 출력을 포함하지 않는다.
- `orm_r1` 과 `orm_r2` 는 같은 학습 데이터 파일을 읽었고 **학습 시 판정 stance 만 다르다**
  (r1 = harsh, r2 = plain). 최종 채택은 r2 이며 근거는 내부 검증셋 측정이다.

따라서 규정 5.2c 가 요구하는 "사용한 외부 데이터셋"은 **최종 구성 기준 없음**이다.
다만 탐색 과정에서 사용하고 폐기한 것들을 아래에 전부 명시한다.

---

## 1. 탐색 과정에서 사용했으나 최종 채택하지 않은 외부 데이터셋 (규정 5.2)

| 데이터셋 | 용도 | 사용량 | 접근성 | 결과 |
|---|---|---|---|---|
| `nvidia/OpenMathInstruct-2` | 외부 SFT r1 | 20,000건 (`train_1M` split) | HuggingFace 공개·무료 · **CC-BY-4.0** | 샘플 정확도 −0.1239. **폐기** |
| `microsoft/orca-math-word-problems-200k` | 외부 SFT r2·r3 | 20,000건 (`train` split) | HuggingFace 공개·무료 · **MIT** | −0.0787 / −0.0554. **폐기** |
| `AI-MO/NuminaMath-1.5` | (코드에 등록만 함) | **0건** | HuggingFace 공개·무료 · Apache-2.0 | **미사용** |

`NuminaMath-1.5` 는 `src/build_external.py` 의 데이터셋 레지스트리에 항목으로 존재하나
실제로 내려받거나 학습에 사용한 적이 없다. 코드에 이름이 남아 있으므로 투명성을 위해 명시한다.

두 데이터셋 모두 규정 5.2a 의 "모든 참가자가 무료로 동등하게 접근 가능한 공개 데이터"에
해당한다. 유료 구독·특수 라이선스·비공개 협약이 필요한 데이터는 사용하지 않았다(5.2b).

세 번의 외부 SFT 는 모두 베이스 대비 성능이 하락하여 채택하지 않았고,
해당 체크포인트(`ext_r1`, `ext_r2`, `ext_r3`)는 최종 파이프라인에 포함되지 않는다.

**오염 제거**: 두 코퍼스와 대회 데이터의 중복 문항을 사전 제거했다
(orca 672건, omi2 124건). 대회 test 데이터는 어떤 단계에서도 학습에 사용되지 않았다(5.1b).

---

## 2. 학습 데이터 구축에 사용한 상용/무료 API (규정 5.3a)

| 제공자 | 모델 | 용도 | 과금 | 결과 |
|---|---|---|---|---|
| DeepSeek | `deepseek-v4-pro`, `deepseek-v4-flash` | train 문항 풀이 생성 | 유료 (약 $22) | 교사 SFT. **폐기** |
| NVIDIA NIM | `nemotron-3-super-120b-a12b` 계열 | train 문항 풀이 생성 | 무료 티어 | 교사 SFT. **폐기** |

**적용 범위 — 이것이 핵심이다.**

- 외부 API 는 **주최 측 제공 train 데이터의 문항에만** 사용했다.
- `deep_chal_math_test.csv` 및 `deep_chal_math_leaderboard_filtered.csv` 의 문항은
  **어떤 외부 API·검색 엔진·외부 서비스에도 입력하지 않았다** (규정 5.3b, 5.3c 준수).
- 이는 프로젝트 초기에 명문화한 운영 원칙이며 전 기간 유지했다:
  *"외부 자원은 train 문항에만. 리더보드와 test 문항은 기기 밖으로 나가지 않는다."*

교사 생성 데이터로 학습한 두 체크포인트(`teach_r1`, `teach_r3`)는 사전등록된 게이트를
통과하지 못해 채택하지 않았다. 최종 구성에 교사 유래 정보는 포함되지 않는다.

### 2.1 운영진 2026-08-28 판정에 대한 대조

디스코드 질의응답에서 김진찬 운영진이 다음과 같이 판정했다.

- 타 모델로 학습 데이터(풀이 등)를 생성하여 활용: **허용**
- 학습 과정에서 타 모델이 풀이의 적절성을 평가·판정하는 역할로 개입: **금지**
- 정답 라벨(ground truth) 기반의 강화학습: **허용**

우리 파이프라인의 해당 지점을 전부 대조한다.

| 지점 | 구현 | 판정 |
|---|---|---|
| ORM 라벨 생성 | `src/build_orm_data.py` — 추출된 답을 주최 측 gold 와 정수 일치 비교(`v == g`)하여 1/0 부여 | 정답 라벨 기반. **허용** |
| ORM 학습 풀이 | `out/gen_rft_r1.jsonl` — 베이스 모델이 직접 생성 | 외부 모델 무관 |
| `src/teach_gen.py` | 교사 모델이 train 문항 풀이 **생성만** 수행. 교사에게 정답을 알려주지 않으며 gold 가 독립 필터로 남는다 | 학습 데이터 생성. **허용** (해당 체크포인트는 미채택) |
| `src/val_audit.py` | 교사가 val 문항을 독립적으로 풀어 라벨 오류를 감사. 출력은 측정에만 사용하고 학습에 투입하지 않음 | 학습 개입 아님 |
| `label_suspect_*.csv` | 베이스 모델 자신의 만장일치 불일치로 생성 | 외부 모델 무관 |
| 최종 제출 구성 | 베이스 무수정 생성 + 동일 베이스 LoRA 채점 | 외부 호출 0 |

**외부 모델이 베이스 모델의 풀이를 O/X 판정한 지점은 파이프라인 어디에도 없다.**

---

## 3. 주최 측 제공 데이터의 사용 (규정 5.1)

| 파일 | 용도 |
|---|---|
| `deep_chal_math_dataset_train.csv` | ORM 학습 데이터 생성, 내부 검증셋 분리, 교사 생성 대상 |
| `deep_chal_math_dataset_leaderboard_filtered.csv` | 리더보드 제출용 추론 **입력으로만** 사용 (8/3 14:00 정제판) |
| `deep_chal_math_dataset_test.csv` | 최종 제출용 추론 **입력으로만** 사용 (8/31 공개) |
| `train_filtered_ids.csv` | 운영진 제공 오류 문항 목록. 학습 데이터에서 제외 |

제공 데이터는 전부 **CSV** 이며 필드는 `id` · `question` · `answer` 세 개다.
`answer` 는 정수로 통일돼 있고 평가셋에서는 비어 있다.

- `test.parquet` 의 문제는 학습 데이터로 사용하지 않았다 (5.1b).
- 내부 검증셋(val 1,000문항)은 train 에서 분리했으며, 학습 데이터와의 교집합이
  **0** 임을 감사로 확인했다(학습 데이터 5종 전부 대조).

### 라벨 오류 필터링

- 운영진 제공 `train_filtered_ids.csv` 의 **627문항**(헤더 제외)을 학습 데이터에서 제외했다.
- 추가로 자체 일관성 검사로 **440문항**을 격리했다. 이 격리 집합과 공식 목록의 교집합은 0이며,
  내부 검증셋과의 교집합도 0이다.
- 디스코드에서 운영진이 타당성을 인정한 참가자 제보분(mislabel 442 + illposed 623)은
  **목록 파일이 배포되지 않아 미적용**이다. 대회 자료 폴더에도 해당 파일이 없음을 확인했다.
  최종 구성의 생성 모델은 무학습이므로 이 미적용이 영향을 주는 범위는 ORM 학습 데이터에 한정된다.

---

## 4. 규정 준수 확인

| 항목 | 규정 | 준수 |
|---|---|---|
| 베이스 외 모델 가중치 로드·병합 | 4.3 | 없음. 최종 구성은 베이스 + 자체 학습 LoRA 검증기뿐 |
| 추론 시 외부 모델 앙상블 | 4.3 | 없음 |
| 사전학습 처음부터 수행 | 4.3 | 없음 |
| test 문항을 학습에 사용 | 5.1b | 없음 |
| 유료·비공개 데이터 사용 | 5.2b | 없음 |
| 외부 데이터셋 목록 명시 | 5.2c | 본 문서 §1 |
| API 로 test 답 직접 생성 | 5.3b | 없음 |
| test 문항을 검색·외부 서비스 입력 | 5.3c | 없음 |
| 추론 시 인터넷 접속 | 6a | 없음. 전 과정 오프라인 |
| 추론 시 코드 실행·툴 호출 | 운영진 8/1 | 없음. 모델 출력만으로 답 도출 |
| 테스트 타임 기법 | 6c | Self-consistency(maj@64) + 자체 검증기 Best-of-N. 허용 범위 |
| 리더보드 probing | 10.1d | 없음. 총 8회 제출, 그중 3회는 동일 구성 재현성 측정용 |
| 학습 과정에 타 모델 개입 | 운영진 8/28 | 없음. §2.1 참조 |

---

## 5. 제출 파일 형식에 관한 확인 사항

규정 8.1b 는 컬럼명을 **`ID`** 와 `answer` 로 명시하고 예시에 `prob_0001` 형태의 id 를 든다.
그러나 실제 제공 데이터의 필드명은 소문자 **`id`** 이고 값은 `train-000000` 형태다.
Kaggle 리더보드는 소문자 `id` 로 제출한 파일을 정상 채점했다.

규정 예시가 일반 템플릿으로 보이나, 최종 제출은 구글 폼 + 운영진 직접 채점이므로
**8/31 데이터셋 공개 시 함께 안내되는 형식 지침을 따른다.**
`finalize.py` 로 컬럼명을 변환할 수 있게 준비해두었다.
